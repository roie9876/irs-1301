---
phase: 02-pdf-extraction-form-106
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - backend/app/main.py
  - frontend/src/App.tsx
  - frontend/src/components/layout/AppLayout.tsx
autonomous: false
requirements: [DOC-01, DOC-02, DOC-09, DOC-10]

must_haves:
  truths:
    - "User can navigate to /documents via header nav link 'מסמכים'"
    - "Full upload → extraction → review → correction flow works end-to-end"
    - "Multiple Form 106 uploads show aggregated totals"
    - "Saved corrections persist when leaving and returning to the page"
  artifacts:
    - path: "backend/app/main.py"
      provides: "Documents router wired to FastAPI app"
      contains: "documents.router"
    - path: "frontend/src/App.tsx"
      provides: "/documents route pointing to DocumentsPage"
      contains: "DocumentsPage"
    - path: "frontend/src/components/layout/AppLayout.tsx"
      provides: "מסמכים nav link in header"
      contains: "מסמכים"
  key_links:
    - from: "frontend/src/components/layout/AppLayout.tsx"
      to: "/documents"
      via: "Link component"
      pattern: "to=.*documents"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/DocumentsPage.tsx"
      via: "Route element"
      pattern: "Route.*documents.*DocumentsPage"
    - from: "backend/app/main.py"
      to: "backend/app/routers/documents.py"
      via: "include_router"
      pattern: "include_router.*documents"
---

<objective>
Wire backend and frontend together: register the documents router, add the /documents route and navigation link, then verify the full flow with a human checkpoint.

Purpose: Connects Plans 02-01 (backend) and 02-02 (frontend) into a working feature. This is the integration layer that makes the upload → extract → review flow accessible to the user.
Output: Fully wired application with /documents page accessible from header navigation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pdf-extraction-form-106/02-CONTEXT.md

@.planning/phases/02-pdf-extraction-form-106/02-01-SUMMARY.md
@.planning/phases/02-pdf-extraction-form-106/02-02-SUMMARY.md

@backend/app/main.py
@frontend/src/App.tsx
@frontend/src/components/layout/AppLayout.tsx

<interfaces>
<!-- From Plan 02-01 -->
From backend/app/routers/documents.py:
```python
router = APIRouter(tags=["documents"])
# POST /documents/upload
# GET /documents
# PUT /documents/{doc_id}
```

<!-- From Plan 02-02 -->
From frontend/src/pages/DocumentsPage.tsx:
```typescript
export function DocumentsPage(): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire router, route, and navigation</name>
  <files>backend/app/main.py, frontend/src/App.tsx, frontend/src/components/layout/AppLayout.tsx</files>
  <action>
1. **backend/app/main.py** — Add documents router (per D-08, following settings.router pattern):
   - Add import: `from app.routers import settings, documents`
   - Add: `app.include_router(documents.router, prefix="/api")` after the settings router line.

2. **frontend/src/App.tsx** — Add /documents route (per D-08):
   - Import: `import { DocumentsPage } from './pages/DocumentsPage'`
   - Add route inside `<Routes>`: `<Route path="/documents" element={<DocumentsPage />} />`
   - Place it after the `/settings` route.

3. **frontend/src/components/layout/AppLayout.tsx** — Add nav link (per D-08):
   - Import: `import { Settings, FileText } from 'lucide-react'` (add FileText)
   - Add a link to `/documents` BEFORE the settings link in the `<nav>` section:
     ```tsx
     <Link
       to="/documents"
       className={`inline-flex items-center gap-2 rounded-md px-3 py-2 text-sm font-medium transition-colors hover:bg-accent hover:text-accent-foreground ${
         location.pathname === '/documents' ? 'bg-accent text-accent-foreground' : ''
       }`}
     >
       <FileText className="h-4 w-4" />
       מסמכים
     </Link>
     ```

4. Verify the app starts without errors:
   ```bash
   cd /Users/robenhai/irs-1301 && make run
   ```
   Check that http://localhost:5173/documents loads and the nav link appears.
  </action>
  <verify>
    <automated>cd /Users/robenhai/irs-1301 && .venv/bin/python -m pytest backend/tests/ -v --tb=short && cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Documents router wired in main.py. /documents route added in App.tsx. "מסמכים" nav link with FileText icon added to AppLayout header. All tests pass, TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full upload-extract-review flow</name>
  <action>
    What was built: Complete Form 106 upload and extraction flow — backend PDF extraction + LLM structuring + frontend upload UI with review and aggregation.

    How to verify:
    1. Start the app: `make run` (terminal should already be running)
    2. Open http://localhost:5173 — verify "מסמכים" link appears in header next to "הגדרות"
    3. Click "מסמכים" — verify you see the upload drop zone with Hebrew text
    4. Upload a Form 106 PDF (drag-and-drop or click to browse):
       - Verify extraction progress indicator shows
       - Verify extracted data appears in an editable table with Hebrew labels
       - Verify confidence badges show in color (green/yellow/red)
    5. Edit a field value and click "שמור תיקונים" — verify change saves
    6. Upload a second Form 106 (different employer) — verify:
       - Second card appears with its own extraction
       - Aggregation summary section appears at bottom with totals
    7. Refresh the page — verify existing extractions reload from disk (per D-11)
    8. Try uploading a non-PDF file — verify error message appears on file card

    Resume signal: Type "approved" or describe issues to fix
  </action>
  <verify>Human approval of the full flow</verify>
  <done>User has verified: upload works, extraction displays correctly, editing saves, aggregation sums, persistence on reload, error handling for invalid files</done>
</task>

</tasks>

<verification>
- All backend tests pass (`pytest backend/tests/ -v`)
- Frontend compiles (`tsc --noEmit`)
- Nav link "מסמכים" visible in header
- Full upload → extract → review → correct flow works
- Multiple uploads aggregate correctly
- Data persists across page reload
- Error handling for invalid files works
</verification>

<success_criteria>
- /documents page accessible from header navigation
- Upload, extraction, review, correction flow works end-to-end
- Multiple employer aggregation works
- Corrections persist when returning to the page
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-extraction-form-106/02-03-SUMMARY.md`
</output>
